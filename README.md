# Effective Mobile Website Test Automation

Проект автоматизации тестирования сайта effective-mobile.ru с использованием Python, Playwright и Allure.

## 🌐 Совместимость

- ✅ **Windows 10/11** - полная поддержка
- ✅ **Linux (Ubuntu, CentOS)** - полная поддержка
- ✅ **macOS** - полная поддержка
- ✅ **Docker** - кросс-платформенная поддержка

> 💡 **Примечание для Windows:** Рекомендуется использовать PowerShell или Git Bash для команд. Makefile может не работать нативно в Windows - используйте PowerShell скрипты или WSL.

##  Содержание

- [Обзор проекта](#обзор-проекта)
- [Технологический стек](#технологический-стек)
- [Структура проекта](#структура-проекта)
- [Установка и настройка](#установка-и-настройка)
- [Запуск тестов](#запуск-тестов)
- [Просмотр отчетов](#просмотр-отчетов)
- [Docker](#docker)
- [Конфигурация](#конфигурация)
- [Написание тестов](#написание-тестов)
- [Лучшие практики](#лучшие-практики)

##  Обзор проекта

Данный проект предназначен для автоматизированного тестирования главной страницы сайта effective-mobile.ru. Тесты проверяют:

- Загрузку главной страницы
- Переходы по основным разделам навигации (О нас, Контакты, Услуги, Карьера, Блог)
- Корректность URL после навигации
- Визуальное представление элементов

##  Технологический стек

- **Python 3.10+** - основной язык программирования
- **Playwright** - библиотека для автоматизации браузера
- **Pytest** - фреймворк для запуска тестов
- **Allure** - генерация отчетов о тестировании
- **Page Object Pattern** - паттерн для организации тестового кода
- **Docker** - контейнеризация для удобного развертывания

##  Структура проекта

```
effective-mobile-tests/
├── pages/                  # Page Object классы
│   ├── __init__.py
│   ├── base_page.py       # Базовый класс страницы
│   └── main_page.py       # Главная страница
├── tests/                  # Тестовые файлы
│   ├── __init__.py
│   └── test_main_page_navigation.py
├── utils/                  # Вспомогательные утилиты
│   ├── __init__.py
│   └── test_helpers.py
├── config/                 # Конфигурационные файлы
│   ├── __init__.py
│   └── config.py
├── allure-results/         # Результаты тестов для Allure
├── allure-reports/        # Сгенерированные отчеты
├── screenshots/           # Скриншоты при ошибках
├── requirements.txt       # Зависимости Python
├── pytest.ini            # Конфигурация Pytest
├── conftest.py           # Фикстуры Pytest
├── Dockerfile            # Конфигурация Docker
├── docker-compose.yml    # Docker Compose конфигурация
├── .env.example          # Пример переменных окружения
└── README.md             # Документация
```

##  Установка и настройка

### Локальная установка

1. **Клонирование репозитория:**
   ```bash
   git clone https://github.com/dekich192/aqa_test_task.git
   cd aqa_test_task
   ```

2. **Создание виртуального окружения:**
   ```bash
   python -m venv venv
   
   # Windows
   venv\Scripts\activate
   
   # Linux/Mac
   source venv/bin/activate
   ```

3. **Установка зависимостей:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Установка браузеров Playwright:**
   ```bash
   playwright install chromium
   playwright install-deps chromium
   ```

5. **Настройка переменных окружения:**
   ```bash
   # Скопировать файл примера
   cp .env.example .env
   
   # Отредактировать .env при необходимости
   ```

### Требования

- **Python 3.10+** (рекомендуется 3.11)
- **Chromium браузер** (устанавливается автоматически через Playwright)
- **Не менее 2GB** свободного дискового пространства
- **Git** для клонирования репозитория

#### Windows требования:
- **Windows 10/11** с обновлениями
- **PowerShell 5.1+** или **Windows Terminal**
- **Microsoft Visual C++ Redistributable** (для некоторых пакетов)
- **Docker Desktop** (опционально, для контейнеризации)

##  Запуск тестов

### Запуск всех тестов

```bash
pytest
```

### Запуск с отчетом Allure

```bash
pytest --alluredir=./allure-results
```

### Запуск конкретного теста

```bash
pytest tests/test_main_page_navigation.py::TestMainPageNavigation::test_main_page_load
```

### Запуск в headed режиме (с видимым браузером)

```bash
pytest --headed
```

### Запуск с определенным браузером

```bash
pytest --browser=chromium
pytest --browser=firefox
pytest --browser=webkit
```

### Дополнительные опции

```bash
# С подробным выводом
pytest -v

# С параллельным выполнением
pytest -n auto

# С повторными попытками при неудаче
pytest --reruns 2

# Комбинация опций
pytest -v --alluredir=./allure-results --headed --browser=chromium
```

### 🪟 Windows специфичные команды

В **PowerShell** или **Windows Terminal**:

```powershell
# Активация виртуального окружения
venv\Scripts\activate.ps1

# Запуск тестов
pytest

# Запуск с отчетом
pytest --alluredir=./allure-results

# Генерация отчета
allure generate allure-results -o allure-reports --clean

# Просмотр отчета
allure serve allure-results
```

В **CMD**:

```cmd
# Активация виртуального окружения
venv\Scripts\activate.bat

# Запуск тестов
pytest
```

**Использование Python скрипта (рекомендуется для Windows):**

```powershell
# Проверка установки и запуск
python test_setup.py

# Запуск с генерацией отчета
python run_tests.py --generate-report

# Запуск с просмотром отчета
python run_tests.py --generate-report --serve-report
```

##  Просмотр отчетов

### Генерация и просмотр Allure отчета

1. **Запуск тестов:**
   ```bash
   pytest --alluredir=./allure-results
   ```

2. **Генерация отчета:**
   ```bash
   allure generate allure-results -o allure-reports --clean
   ```

3. **Просмотр отчета:**
   ```bash
   allure serve allure-results
   ```

   Отчет откроется в браузере по адресу http://localhost:8080

### Просмотр в реальном времени

```bash
# Запуск тестов в фоновом режиме
pytest --alluredir=./allure-results &

# Просмотр отчета с автообновлением
allure serve allure-results
```

##  Docker

### Сборка и запуск через Docker

1. **Сборка образа:**
   ```bash
   docker build -t effective-mobile-tests .
   ```

2. **Запуск тестов в контейнере:**
   ```bash
   docker run --rm -v $(pwd)/allure-results:/app/allure-results effective-mobile-tests
   ```

### Использование Docker Compose

1. **Запуск тестов и отчета:**
   ```bash
   docker-compose up
   ```

2. **Только запуск тестов:**
   ```bash
   docker-compose up web-tests
   ```

3. **Только просмотр отчета:**
   ```bash
   docker-compose up allure-report
   ```

4. **Очистка:**
   ```bash
   docker-compose down
   docker system prune -f
   ```

### Преимущества Docker

- Изолированное окружение
- Автоматическая установка всех зависимостей
- Легкое развертывание на любой машине
- Консистентность между средами

##  Конфигурация

### Переменные окружения (.env)

```bash
BASE_URL=https://www.effective-mobile.ru  # Базовый URL сайта
BROWSER=chromium                          # Браузер для тестов
HEADED=false                              # Видимый браузер (true/false)
TIMEOUT=30000                             # Таймаут в миллисекундах
```

### Конфигурация Pytest (pytest.ini)

```ini
[pytest]
addopts = -v --alluredir=./allure-results --browser=chromium --headed=false
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
```

##  Написание тестов

### Структура теста

```python
import allure
from playwright.sync_api import Page
from pages.main_page import MainPage

@allure.feature("Navigation")
@allure.story("Main Page")
class TestMainPage:
    
    def test_navigation_example(self, page: Page):
        main_page = MainPage(page)
        
        with allure.step("Navigate to main page"):
            main_page.navigate_to_main_page()
        
        with allure.step("Verify page elements"):
            main_page.verify_main_page_elements()
```

### Page Object Pattern

```python
class MainPage(BasePage):
    def __init__(self, page: Page):
        super().__init__(page)
        self.about_us_link = "text=О нас"
    
    @allure.step("Click About Us")
    def click_about_us(self):
        self.click_element(self.about_us_link)
```

### Лучшие практики

1. **Используйте Allure декораторы** для документации тестов
2. **Следуйте Page Object Pattern** для поддержки кода
3. **Добавляйте шаги с allure.step** для детальных отчетов
4. **Используйте фикстуры** для повторного использования кода
5. **Добавляйте скриншоты** при ошибках для отладки

##  Лучшие практики

### Тестирование

- **Изолированные тесты:** Каждый тест должен быть независимым
- **Ясные имена:** Используйте описательные имена тестов
- **Детальные шаги:** Разбивайте тесты на логические шаги
- **Обработка ошибок:** Добавляйте информативные сообщения об ошибках

### Page Object

- **Инкапсуляция:** Скрывайте детали реализации локаторов
- **Переиспользование:** Создавайте переиспользуемые методы
- **Читаемость:** Используйте понятные имена методов
- **Минимизация:** Избегайте лишних абстракций

### Отчетность

- **Структурированные отчеты:** Используйте фичи и истории в Allure
- **Вложения:** Добавляйте скриншоты и HTML содержимое
- **Окружение:** Указывайте версию браузера и параметры теста
- **Анализ:** Используйте отчеты для анализа результатов

##  Troubleshooting

### Частые проблемы

1. **Браузер не найден:**
   ```bash
   playwright install chromium
   ```

2. **Timeout ошибки:**
   - Увеличьте TIMEOUT в .env
   - Проверьте скорость интернет соединения

3. **Элемент не найден:**
   - Проверьте селекторы в Page Object
   - Используйте анализ навигационных ссылок

4. **Проблемы с Docker:**
   ```bash
   docker system prune -f
   docker-compose down --volumes
   ```

### Отладка

1. **Запуск в headed режиме:**
   ```bash
   pytest --headed -s -v
   ```

2. **Паузы в тестах:**
   ```python
   page.pause()  # Добавить в тест для отладки
   ```

3. **Логирование:**
   ```bash
   pytest -v -s --log-cli-level=DEBUG
   ```

##  Контакты и репозиторий

### 📂 GitHub репозиторий
- **URL:** https://github.com/dekich192/aqa_test_task
- **Issues:** https://github.com/dekich192/aqa_test_task/issues
- **Wiki:** https://github.com/dekich192/aqa_test_task/wiki

### 📞 Поддержка

При возникновении вопросов или проблем:

1. Проверьте раздел Troubleshooting
2. Изучите сгенерированные Allure отчеты
3. Просмотрите скриншоты в папке screenshots/
4. Создайте Issue в GitHub репозитории
5. Обратитесь к документации Playwright и Pytest

### 🤝 Вклад в проект

- **Форкните репозиторий** и создайте Pull Request
- **Сообщайте о проблемах** через Issues
- **Предлагайте улучшения** в Discussions
- **Следуйте CONTRIBUTING.md** (если есть)

---

**Версия:** 1.0.0  
**Последнее обновление:** 2024  
**Репозиторий:** https://github.com/dekich192/aqa_test_task
